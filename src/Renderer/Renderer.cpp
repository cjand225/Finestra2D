#include "../../inc/Renderer/Renderer.hpp"

namespace fin{


    void Renderer::init(){

        //drawWin.create(sf::VideoMode(screenWidth, screenHeight,32), "s" , sf::Style::Default);
        //drawWin.setFramerateLimit(60);
        //drawWin.setPosition(sf::Vector2i(0, 0));
        //drawWin.setMouseCursorVisible(false);

        //desktop = sf::VideoMode::getDesktopMode();
        //screenHeight = desktop.height;
        //screenWidth =  desktop.width;
        //BPP = desktop.bitsPerPixel;


       // sf::Mouse::setPosition(sf::Vector2i(0, 0), drawWin);


    }


    void Renderer::clearWindow(){

    }

    void Renderer::setFullscreen(bool toggle){

        if(!toggle){

        }
        else{

        }
    }

}

